version: '3.8'
services:
  db:
    image: mysql:8.0
    container_name: asset-db
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: asset_db
    volumes:
      - ./mysql_data:/var/lib/mysql